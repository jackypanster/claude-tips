# 环境特定配置（每台机器不同）
settings.json
settings.local.json          # 本地权限/env 覆盖，可能含密钥——绝不进 public 仓

# Claude Code 运行时生成
debug/
history.jsonl
shell-snapshots/
stats-cache.json
statsig/
telemetry/
todos/
file-history/
transcripts/                 # 对话记录——私有，绝不公开
stats/                       # 使用统计——私有
tasks/                       # 后台任务运行态——运行时噪声

# 每项目本地状态（memory / 会话数据）——本地加载，不进 public claude-tips
projects/

# 临时备份
*.bak
